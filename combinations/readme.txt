RUNNING PROGRAM:
To run file, execute combinations.exe on the command line.
This can be done by holding shift on the keyboard, right clicking in the explorer,
then selecting "open powershell window here". Then type "c" and push tab until
"combinations.exe" appears as an option the option in the window and press enter.

OTHER:
Program made for windows, not guarenteed to work with other OS

-Keegan Mullins